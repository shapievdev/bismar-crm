<?php

declare(strict_types=1);

namespace App\Http\Requests\User;

/**
 * Отказы формы сотрудника — словами, которые человек читает.
 *
 * Одни и те же поля заполняют дважды: когда сотрудника заводят и когда правят
 * его карточку. Сообщения поэтому одни на оба запроса — разойдись они, и один
 * и тот же занятый номер объяснялся бы на двух экранах по-разному.
 *
 * Написаны они здесь, а не взяты из набора фреймворка, по той же причине, по
 * какой весь интерфейс русский: «The password field must be at least 8
 * characters» посреди русской формы читается как сбой, а не как подсказка. Это
 * не общий перевод приложения — он был бы другой работой (`lang/ru`); это
 * ровно та форма, где отказ видят чаще всего.
 */
final class StaffFieldMessages
{
    /**
     * @return array<string, string>
     */
    public static function all(): array
    {
        return [
            'last_name.required' => 'Укажите фамилию.',
            'first_name.required' => 'Укажите имя.',
            'last_name.max' => 'Фамилия длиннее 255 знаков.',
            'first_name.max' => 'Имя длиннее 255 знаков.',
            'middle_name.max' => 'Отчество длиннее 255 знаков.',
            'job_title.max' => 'Должность длиннее 255 знаков.',

            'email.email' => 'Почта набрана неверно: нужен адрес вида ivanov@bismar.pro.',
            'email.unique' => 'Эта почта уже записана за другим сотрудником.',
            'email.max' => 'Почта длиннее 255 знаков.',

            // Телефон — то, что чаще всего набирают не так: с него входят, и
            // вид у него ровно один.
            'phone.required' => 'Без телефона сотрудник не войдёт: вход идёт по номеру.',
            'phone.regex' => 'Телефон должен быть российским номером: +7 и десять цифр.',
            'phone.unique' => 'Этот номер уже занят: по нему входит другой сотрудник.',

            'password.required' => 'Придумайте пароль: с ним сотрудник войдёт в первый раз.',
            'password.min' => 'Пароль короче восьми знаков.',
        ];
    }
}
